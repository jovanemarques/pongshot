module enums {
    export enum GameObjectType {
        PLANE,
        ISLAND,
        CLOUD,
        OCEAN,
        BUTTON,
        IMAGE,
        PLAYER,
        UNDEFINED,
        NUM_OF_TYPES
    }
}
