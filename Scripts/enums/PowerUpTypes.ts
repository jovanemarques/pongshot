module enums {
    export enum PowerUpTypes {
        ARMOR = "armor",
        ATTACK_SPEED = "boots",
        ATTACK_POWER = "spellScroll",
        TRAP = "trap",

        // This items must be the last ones
        POTION_HP = "hp",
        POTION_XP = "xp"
    }
}
